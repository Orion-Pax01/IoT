This setup simulates a simple Cyber-Physical System (CPS) of controlling an LED with UI.

- CPS system only works on actual hardware, cannot be simulated (wokwi etc.)
- Run code.cpp first, then run interface.py
